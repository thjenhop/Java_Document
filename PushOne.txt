
#include <iostream>
#include <vector>

using namespace std;

class Solution {
public:
    vector<int> plusOne(vector<int>& digits) {

        vector<int> ::iterator i = digits.begin();

        for(i ; i != digits.end() - 1; i++ )
        {

        }
        (*(i))++;
        return digits;


    }
    void print(vector<int>& digits)
    {
        for(vector<int> ::iterator i = digits.begin(); i != digits.end(); i++)
        {
            cout << *i <<  ;
        }

    }
};
int main()
{
    Solution x;
    vector<int> v = {1, 3, 5, 6};
    x.plusOne(v) ;
    x.print(v);
    return 0;
}

